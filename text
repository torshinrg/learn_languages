error: Fields can't be initialized in the constructor if they are final and were already initialized at their declaration. (field_initialized_in_initializer_and_declaration at [learn_languages] lib/presentation/providers/custom_words_provider.dart:17)

here:
CustomWordsProvider(this._repo)
      : _srs = GetIt.instance<ISRSRepository>() {        // ← get via GetIt
    _load();
  }