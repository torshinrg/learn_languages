✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"
🧹 Cleared existing tasks from "tasks" table
✅ Created (or verified) table "tasks"